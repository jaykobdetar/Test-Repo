# Replace 2001 with the actual probe-trusted UID. The user manager and its bus
# must already be running; /run/probe-sandbox is not a session runtime directory.
XDG_RUNTIME_DIR=/run/user/2001
DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/2001/bus
